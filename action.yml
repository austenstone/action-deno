name: Deno Action
author: Austen Stone
description: A Deno Action
branding:
  icon: "plus"
  color: "blue"

inputs:
  github-token:
    description: The GitHub token used to create an authenticated client
    default: ${{ github.token }}
    required: false

runs:
  using: "docker"
  image: "Dockerfile"
